const { getUserByIdHandler } = require("../handlers/users.mjs");

const mockRequest = {};

const mockResponse = {};

describe('get users', () => {
    it('should get user by id', () => {
        getUserByIdHandler()
    })
});
